# Data Contracts

This document defines every file passed between members of the group. A member
reads another member's output by reading the file described here, never by
importing another member's notebook or copying their code.

Any change to a contract must be raised at a standup before it is merged, since
a silent schema change breaks every downstream member without raising an error.

All curated outputs live in `data/curated/` and are regenerated by running the
owning notebook from a clean kernel.

---

## Universal rules

**`sa2_code_2021` is the join key for all geographic aggregation.** It is a nine
character zero padded string and must never be stored or read as an integer.
Converting it to an integer drops leading zeros, and because every Victorian
code begins with the digit 2 the loss is invisible in this state but breaks any
join against a national dataset.

**`property_id` is the join key for all property level data.** It is a string,
unique, and never null. It carries the Domain listing identifier, renamed from
`listing_id` on read.

**Geographic reference: ASGS Edition 3, 2021 SA2 boundaries, GDA2020.** Edition 4
became available from July 2026 but is not used, because the population
projection and income datasets are compiled against 2021 SA2 codes.

**Growth rates are stored as proportions, not percentages.** A value of `0.15`
means fifteen percent.

**Coordinates are EPSG 4326. Distances are computed in EPSG 7855.** In a
geographic CRS the unit is the degree, and a degree of longitude is shorter
than a degree of latitude at Victorian latitudes, so Euclidean distance on raw
coordinates is wrong by a varying amount across the state.

**All distances are in kilometres and all durations in minutes.**

---

## sa2_features.parquet

**Owner:** Member C
**Produced by:** `code/01_external_data.ipynb`
**Rows:** 524, one per Victorian SA2
**Columns:** 38
**Status:** complete, committed to the repository

A companion `sa2_features.gpkg` carries the same rows plus geometry for mapping
and spatial joins. The parquet has no geometry so it stays small and merges
cleanly, and it is the only one of the two committed to git.

### Identifiers

| column | type | unit | notes |
|---|---|---|---|
| `sa2_code_2021` | string | | Primary key. Nine characters, zero padded. |
| `sa2_name_2021` | string | | Display only. Never a join key. |
| `sa3_name` | string | | Parent SA3 region. |
| `sa4_name` | string | | Parent SA4 region. |
| `gcc_name` | string | | `Greater Melbourne` or `Rest of Vic.` |
| `area_sqkm` | float | square kilometres | Null for the two special purpose SA2s. |

### Population, household and dwelling projections

Source: Victoria in Future 2023, Second Release. Four year points only, being
2021, 2026, 2031 and 2036.

| column | type | unit | notes |
|---|---|---|---|
| `population_2021` | float | persons | |
| `population_2026` | float | persons | |
| `population_2031` | float | persons | |
| `population_2036` | float | persons | |
| `households_2026` | float | households | Occupied private dwellings. |
| `households_2036` | float | households | |
| `dwellings_2026` | float | dwellings | Total stock, occupied and unoccupied. |
| `dwellings_2036` | float | dwellings | |
| `population_growth_5y` | float | proportion | 2026 to 2031. |
| `population_growth_10y` | float | proportion | 2026 to 2036. |
| `household_growth_10y` | float | proportion | 2026 to 2036. |
| `dwelling_growth_10y` | float | proportion | 2026 to 2036. |
| `demand_supply_gap_10y` | float | proportion | Household growth minus dwelling growth. See the caveat below. |
| `occupancy_ratio_2026` | float | ratio | Households divided by dwellings. |
| `avg_household_size_2026` | float | persons per household | |
| `population_density_2026` | float | persons per square kilometre | |

### Personal income

Source: ABS Personal Income in Australia, 2022 to 2023, released 14 November
2025. Gross pre tax annual income from all sources. Government pensions and
allowances are excluded from the ABS definition.

| column | type | unit | notes |
|---|---|---|---|
| `earners` | float | persons | |
| `median_age_earners` | float | years | |
| `income_median` | float | AUD per year | Prefer this over the mean. |
| `income_mean` | float | AUD per year | Sensitive to a few very high earners. |
| `income_growth_5y` | float | proportion | 2018-19 to 2022-23. |
| `income_skew_ratio` | float | ratio | Mean divided by median. Toorak reaches 2.57 while Yarraville sits at 1.22 despite similar medians. |

### School and station counts

| column | type | unit | notes |
|---|---|---|---|
| `n_schools` | int | count | Campuses inside the SA2. Median 4, maximum 14. |
| `n_primary_schools` | int | count | Includes Pri/Sec campuses, so primary and secondary counts overlap and do not sum to the total. |
| `n_secondary_schools` | int | count | Includes Pri/Sec campuses. |
| `n_government_schools` | int | count | |
| `n_stations` | int | count | Train stations only. Median 1, maximum 5. |
| `n_stations_both_networks` | int | count | Served by both regional and suburban rail. |

Null counts are filled with zero, unlike the demographic columns where a null
means the value was unavailable rather than zero.

**These counts are weak features on their own.** 310 of 524 SA2s contain no
station and 21 contain no school, and an SA2 without a station may still sit a
few hundred metres from one across its boundary. The property level distances
in `property_geo_features.parquet` are the primary measure of access.

### Quality flags

| column | type | true count | meaning |
|---|---|---|---|
| `is_residential` | bool | 511 | Non zero population, households and dwellings in 2026. The eleven false rows are four airports, three national park areas, two industrial estates and two water or island areas. |
| `is_analysable` | bool | 509 | Residential and 2026 population at least 1000. |
| `income_reliable` | bool | 509 | At least 1000 earners and income not suppressed. The ABS applies this same threshold in its own regional rankings. |
| `is_modelling_ready` | bool | 509 | Both of the above. **Filter on this before modelling.** |

Rows are retained rather than deleted so the count matches the boundary file
and excluded areas can be shaded rather than omitted on a map.

### Known limitations

**Income is a weak proxy for affluence in student dominated SA2s.** Melbourne
CBD North, Melbourne CBD West, Carlton and Clayton North Notting Hill record
the lowest median incomes in the state yet sustain high rents, because their
residents are largely students supported by family or overseas funds rather
than local earnings. A model relying on income alone will underpredict rent
there. Consider an interaction with dwelling density or a student area
indicator.

**Reference periods do not align.** Listings were scraped in 2025, income
covers 2022 to 2023, and projections are anchored on a 2023 base.

**`demand_supply_gap_10y` has little variation.** The median and both
quartiles are exactly zero, because Victoria in Future appears to derive
dwelling counts from household counts using a fixed occupancy assumption for
most regions. Treat it as a secondary feature.

**The fastest growing SA2 is an inner city redevelopment.** Port Melbourne
Industrial is projected to grow 194 percent between 2026 and 2036, driven by
Fishermans Bend. Its rental market is high density apartments and behaves
differently from the outer corridors such as Wallan and Tarneit that fill the
rest of the top ten.

---

## property_geo_features.parquet

**Owner:** Member C
**Produced by:** `code/02_spatial_features.ipynb`
**Rows:** 11945, one per listing
**Columns:** 23
**Status:** complete, built from the scraped listings

One row for every listing in `rental_data/analysis_data`, joined on
`property_id`.

### Identifiers and location

| column | type | unit | notes |
|---|---|---|---|
| `property_id` | string | | The Domain `listing_id`, renamed on read. Unique, never null. |
| `sa2_code_2021` | string | | Foreign key to `sa2_features.parquet`. Assigned by point in polygon. |
| `sa2_name_2021` | string | | Display only. |
| `in_victoria` | bool | | True for every listing. Retained because the pipeline reports rather than drops a point outside every SA2. |

**All 11945 listings matched to an SA2, with none unmatched.** The validation
run on generated coordinates had 30 unmatched of 500, all of which proved to be
in Port Phillip Bay, an artefact of perturbing bayside station coordinates. On
scraped addresses the rate is zero, and a non zero rate in a future batch would
point to a geocoding error worth investigating.

**Suburb names are never used to assign an SA2.** A suburb can span several
SA2s and an SA2 can cover several suburbs, so name matching produces confident
but wrong assignments. The join was validated on 2293 school points, all of
which matched, and on 320 station points, where the only unmatched record was
Albury, correctly identified as lying in New South Wales.

### Melbourne CBD

Reference point is Flinders Street Station at -37.8183, 144.9671.

| column | type | unit | notes |
|---|---|---|---|
| `straight_km_to_cbd` | float | km | Computed locally in EPSG 7855. |
| `route_km_to_cbd` | float | km | Driving route via OpenRouteService. |
| `route_min_to_cbd` | float | minutes | Free flow driving time. |

### Train stations

| column | type | unit | notes |
|---|---|---|---|
| `straight_km_to_nearest_station` | float | km | Measured, from a BallTree over projected coordinates. |
| `est_route_km_to_nearest_station` | float | km | **Estimated, not measured.** Straight line distance multiplied by a calibrated detour index of 1.537. The `est_` prefix marks it as an estimate. |
| `nearest_station_name` | string | | |
| `n_stations_within_1km` | int | count | |
| `n_stations_within_2km` | int | count | |

### Schools

| column | type | unit | notes |
|---|---|---|---|
| `straight_km_to_nearest_school` | float | km | Any campus, all sectors and types. |
| `nearest_school_name` | string | | |
| `n_schools_within_1km` | int | count | |
| `n_schools_within_2km` | int | count | |

### Green space

| column | type | unit | notes |
|---|---|---|---|
| `straight_km_to_nearest_park` | float | km | Any `leisure=park` feature. |
| `straight_km_to_nearest_named_park` | float | km | Named parks only, a proxy for significant public open space. Always greater than or equal to the column above, since named parks are a subset. |
| `nearest_named_park` | string | | |
| `n_parks_within_500m` | int | count | |
| `n_parks_within_1km` | int | count | |

### Retail

| column | type | unit | notes |
|---|---|---|---|
| `straight_km_to_nearest_mall` | float | km | Better read as distance to the nearest retail cluster than to a major shopping centre. |
| `nearest_mall_name` | string | | |

### Route distance strategy

The specification asks for proximity by road route rather than straight line.
Routing every listing to every station is not feasible on the free tier, which
allows 500 matrix calls per day, so a two tier approach is used.

**Melbourne CBD is routed directly.** At twenty or thirty kilometres, river
crossings, freeway access and one way systems shift the answer by kilometres.
The free tier caps a matrix at 3500 origin destination pairs, so the 11945
listings are sent in four batches of up to 3000 against the single CBD
destination, costing four of the 500 daily calls. Each batch caches
independently, so an interrupted run resumes without respending quota.

**Station distance is estimated from a calibrated detour index.** A random
sample of 50 listings was routed to its nearest station and the ratio of route
distance to straight line distance measured. After excluding five implausible
ratios above three, the median is **1.537** with an interquartile range of
1.287 to 2.019. A station one kilometre away in a straight line is on average
about 1.5 kilometres by road.

**The index is a finding in its own right.** It quantifies how far the
Melbourne road network departs from direct access to rail.

### The snapping guard, and why it is retained

A route cannot be shorter than the straight line between its endpoints, so a
ratio below one proves that OpenRouteService routed between different points
from the ones requested. The service snaps each endpoint to the nearest
routable road, and where a coordinate sits away from the network the snap moves
it. No error is raised when this happens.

**The validation run showed the problem clearly.** Two of fifty calibration
pairs on generated coordinates returned a ratio below one, at straight line
distances of 0.91 and 1.38 kilometres. Those test points had been perturbed
into the bay or away from any road.

**The scraped listings show none of it.** No calibration pair falls below one,
the smallest ratio being 1.010, and none of the routed CBD distances is shorter
than its straight line. Scraped addresses sit on streets, so the snap moves
them by metres rather than hundreds of metres.

The guard remains in the final assertions. It cost nothing, it caught a real
problem during validation, and it will catch a geocoding error in any future
batch of listings.

### Known limitations

**Park counts slightly overstate the number of distinct parks.** A linear
reserve such as Melbourne Water Pipe Reserve is mapped as 32 adjacent ways,
each counted separately. Deduplicating by name would be worse: Apex Park
appears 46 times across Victoria as 46 genuinely separate parks in different
towns, built by the same service club, not one fragmented park.

**No park area is available.** Full polygon geometry would multiply the
Overpass response by about two orders of magnitude. A named park is used as a
proxy for a significant one, since named parks in OpenStreetMap are generally
formally managed public spaces while unnamed ones are typically verges and
median strips.

**`shop=mall` is applied loosely.** Some records are individual shops, such as
John Thomson & Co in western Victoria.

### Guarantees

- Exactly one row per `property_id` in the input
- `sa2_code_2021` is non null for every row
- No routed distance is shorter than its straight line equivalent
- `straight_km_to_nearest_named_park` is never below `straight_km_to_nearest_park`
- All distance columns are non negative
- A listing outside every SA2 would be flagged rather than silently dropped or
  snapped to the nearest polygon, since snapping would fabricate a location and
  hide a genuine coordinate error

---

## Supporting point files

In `data/raw/` rather than `data/curated/`, since they are inputs to the
property level build rather than deliverables.

| file | contents |
|---|---|
| `vic_school_points.parquet` | 2293 school campuses with coordinates and SA2. |
| `vic_train_stations.parquet` | 320 stations. Albury lies in New South Wales and is flagged by `in_victoria` being false, retained because for listings near Wodonga it may be the nearest station. |
| `osm_parks_vic.parquet` | 17005 green space records, 8042 named. |
| `osm_malls_vic.parquet` | 383 shopping centres inside Victoria. |
| `sa2_vic_boundaries.gpkg` | 524 SA2 polygons. |
| `vif_sa2_projections.parquet` | Intermediate projection table. |

---

## rental_data/analysis_data

**Owner:** Members A and B
**Produced by:** `code/plant_scraper.ipynb` and `code/preprocessing.ipynb`
**Rows:** 11945
**Status:** complete

A Spark parquet directory rather than a single file, so a reader points at the
directory and pandas reads every part file inside it.

Member C consumes only three columns, and renames them on read to match the
names used throughout this document.

| upstream name | name used downstream |
|---|---|
| `listing_id` | `property_id` |
| `lat` | `latitude` |
| `lon` | `longitude` |

The remaining columns include `weekly_rent`, `bedrooms`, `bathrooms`,
`carspaces`, `suburb`, `postcode`, `primary_type` and `date_listed`, which the
modelling work reads directly from this file.

`primary_type` takes the values House, Apartment, Townhouse/Villa and New
Developments, which differ from the values proposed in an earlier draft of this
document. The upstream values are authoritative.

---

## predictions.parquet

**Owner:** Member D
**Produced by:** the modelling notebook
**Status:** pending

Expected to carry a predicted rent per listing or per SA2, together with a
feature importance table addressing the first research question.

Join `rental_data/analysis_data` to `property_geo_features.parquet` on
`property_id`, then to `sa2_features.parquet` on `sa2_code_2021`. Filter on
`is_modelling_ready` before fitting.

---

## Security

API keys are read from a local `.env` file listed in `.gitignore` and never
committed. No credential appears in any notebook, script, output or commit
message. Notebook outputs are saved into the `.ipynb` file, so a key is never
printed even for debugging: only its length and first few characters are shown
when confirming that it loaded.

The HeiGIT terms of service state that an API key belongs to one person and
must not be used client side in an application, since inspecting the requests
would leak it. Server side use with the key held outside the codebase is the
required pattern.

Every member confirms with `git status` that `.env` is absent from the staging
area before committing.

Raw downloads under `data/landing/` and intermediate files under `data/raw/`
are excluded from version control and regenerated by running the notebooks.
Each landing subdirectory carries a `SOURCE.md` recording origin, reference
period, download date and licence.

Two curated files are committed by exception, being `sa2_features.parquet` and
`property_geo_features.parquet`. Both are small, both are stable deliverables,
and committing them saves every downstream member from downloading five
external datasets and rerunning the pipeline.

API responses are cached under `cache/`, also excluded. The OpenRouteService
free tier allows 500 matrix calls per day, so an unguarded rerun could exhaust
a day of quota and block work until the reset.

The PTV archive is read directly from `gtfs.zip` and must not be unpacked.
Unpacking writes over three hundred megabytes the project never reads and adds
a folder level that breaks the archive paths.

## Attribution

Output published from this project must credit OpenStreetMap contributors, as
required by the Open Database License covering the park and retail data.